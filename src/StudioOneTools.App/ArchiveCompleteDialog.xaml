<Window x:Class="StudioOneTools.App.ArchiveCompleteDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Delete Original Folder?"
        Height="220"
        Width="450"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Margin="0,0,0,12"
                   FontSize="14"
                   FontWeight="SemiBold"
                   Text="Archive created successfully!" />

        <TextBlock Grid.Row="1"
                   Margin="0,0,0,16"
                   TextWrapping="Wrap"
                   Text="Do you want to delete the original song folder?" />

        <CheckBox Grid.Row="2"
                  VerticalAlignment="Top"
                  x:Name="OpenFolderCheckBox"
                  IsChecked="False"
                  Content="Open Archive Folder" />

        <StackPanel Grid.Row="3"
                    HorizontalAlignment="Right"
                    Orientation="Horizontal">
            <Button Padding="16,6"
                    Margin="0,0,8,0"
                    x:Name="NoButton"
                    Content="No"
                    Click="NoButton_OnClick" />
            <Button Padding="16,6"
                    IsDefault="True"
                    x:Name="YesButton"
                    Content="Yes"
                    Click="YesButton_OnClick" />
        </StackPanel>
    </Grid>
</Window>
