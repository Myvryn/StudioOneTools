<Window x:Class="StudioOneTools.App.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Settings"
        Height="300"
        Width="600"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Margin="0,0,0,16"
                   FontSize="18"
                   FontWeight="SemiBold"
                   Text="Application Settings" />

        <!-- Default Song Folder -->
        <Grid Grid.Row="1"
              Margin="0,0,0,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="160" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Column="0"
                       Margin="0,0,12,0"
                       VerticalAlignment="Center"
                       Text="Default song folder" />

            <TextBox x:Name="DefaultSongFolderTextBox"
                     Grid.Column="1"
                     Padding="8,6"
                     Text="{Binding DefaultSongFolder, UpdateSourceTrigger=PropertyChanged}" />

            <Button Grid.Column="2"
                    Margin="12,0,0,0"
                    Padding="16,6"
                    Content="Browse..."
                    Click="BrowseSongFolderButton_OnClick" />
        </Grid>

        <!-- Default Archive Folder -->
        <Grid Grid.Row="2"
              Margin="0,0,0,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="160" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Column="0"
                       Margin="0,0,12,0"
                       VerticalAlignment="Center"
                       Text="Default archive folder" />

            <TextBox x:Name="DefaultArchiveFolderTextBox"
                     Grid.Column="1"
                     Padding="8,6"
                     Text="{Binding DefaultArchiveFolder, UpdateSourceTrigger=PropertyChanged}" />

            <Button Grid.Column="2"
                    Margin="12,0,0,0"
                    Padding="16,6"
                    Content="Browse..."
                    Click="BrowseArchiveFolderButton_OnClick" />
        </Grid>

        <!-- Debug Mode -->
        <CheckBox Grid.Row="3"
                  Margin="0,0,0,12"
                  VerticalAlignment="Top"
                  IsChecked="{Binding DebugMode}"
                  Content="Enable Debug Mode (generates _DEBUG folder with XML files)" />

        <!-- Buttons -->
        <StackPanel Grid.Row="4"
                    HorizontalAlignment="Right"
                    Orientation="Horizontal">
            <Button Padding="16,6"
                    Margin="0,0,8,0"
                    IsCancel="True"
                    Content="Cancel"
                    Click="CancelButton_OnClick" />
            <Button Padding="16,6"
                    IsDefault="True"
                    Content="OK"
                    Click="OKButton_OnClick" />
        </StackPanel>
    </Grid>
</Window>
