<Window x:Class="StudioOneTools.App.BackupWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Studio Pro Tools - Song Backup"
        Height="640"
        Width="1000"
        MinHeight="500"
        MinWidth="760"
        WindowStartupLocation="CenterScreen"
        Loaded="Window_OnLoaded">

    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisibility" />

        <Style x:Key="PrimaryButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#FF107C41" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Border"
                                Background="{TemplateBinding Background}"
                                CornerRadius="3">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"
                                              Margin="{TemplateBinding Padding}" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#FF0D6B38" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#FF0A5A2F" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="Border" Property="Background" Value="#FFC0D8CA" />
                                <Setter Property="Foreground" Value="#FF8AAE9E" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Title with green icon -->
        <StackPanel Grid.Row="0"
                    Orientation="Horizontal"
                    Margin="0,0,0,14">
            <TextBlock FontFamily="Segoe MDL2 Assets"
                       FontSize="28"
                       Foreground="#FF107C41"
                       VerticalAlignment="Center"
                       Margin="0,0,10,0"
                       Text="&#xE74E;" />
            <TextBlock FontSize="24"
                       FontWeight="SemiBold"
                       VerticalAlignment="Center"
                       Text="Song Backup" />
        </StackPanel>

        <!-- Songs root folder row -->
        <Grid Grid.Row="1"
              Margin="0,0,0,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="110" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Column="0"
                       VerticalAlignment="Center"
                       Text="Songs folder" />

            <TextBox x:Name="RootFolderPathTextBox"
                     Grid.Column="1"
                     Padding="8,6"
                     Text="{Binding RootFolderPath, UpdateSourceTrigger=PropertyChanged}"
                     TextChanged="RootFolderPathTextBox_OnTextChanged" />

            <Button Grid.Column="2"
                    Margin="12,0,0,0"
                    Padding="16,6"
                    Content="Browse..."
                    Click="BrowseSongFolderButton_OnClick" />
        </Grid>

        <!-- Song folders DataGrid -->
        <DataGrid x:Name="SongFoldersDataGrid"
                  Grid.Row="2"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  CanUserResizeRows="False"
                  SelectionMode="Single"
                  HeadersVisibility="Column"
                  RowBackground="White"
                  AlternatingRowBackground="#FFF0F8F2"
                  ItemsSource="{Binding SongFolders}">
            <DataGrid.Columns>

                <!-- Single-click checkbox via template column -->
                <DataGridTemplateColumn Width="44"
                                        CanUserResize="False"
                                        CanUserSort="False">
                    <DataGridTemplateColumn.HeaderTemplate>
                        <DataTemplate>
                            <TextBlock Text="&#x2714;"
                                       HorizontalAlignment="Center"
                                       ToolTip="Include in backup" />
                        </DataTemplate>
                    </DataGridTemplateColumn.HeaderTemplate>
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <CheckBox IsChecked="{Binding IsSelected, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}"
                                      HorizontalAlignment="Center"
                                      VerticalAlignment="Center" />
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <DataGridTextColumn Header="Song Folder"
                                    Binding="{Binding FolderName}"
                                    Width="2*"
                                    IsReadOnly="True" />

                <DataGridTextColumn Header="Path"
                                    Binding="{Binding FolderPath}"
                                    Width="*"
                                    IsReadOnly="True" />

            </DataGrid.Columns>
        </DataGrid>

        <!-- Backup destination row -->
        <Grid Grid.Row="3"
              Margin="0,12,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="110" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Column="0"
                       VerticalAlignment="Center"
                       Text="Backup to" />

            <TextBox x:Name="BackupFolderPathTextBox"
                     Grid.Column="1"
                     Padding="8,6"
                     Text="{Binding BackupFolderPath, UpdateSourceTrigger=PropertyChanged}" />

            <Button Grid.Column="2"
                    Margin="12,0,0,0"
                    Padding="16,6"
                    Content="Browse..."
                    Click="BrowseBackupFolderButton_OnClick" />
        </Grid>

        <!-- Action buttons row -->
        <Grid Grid.Row="4"
              Margin="0,8,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <Button Grid.Column="0"
                    Padding="16,6"
                    IsEnabled="{Binding HasSongFolders}"
                    Content="Select All"
                    Click="SelectAllButton_OnClick" />

            <Button Grid.Column="1"
                    Margin="8,0,0,0"
                    Padding="16,6"
                    IsEnabled="{Binding HasSongFolders}"
                    Content="Deselect All"
                    Click="DeselectAllButton_OnClick" />

            <Button Grid.Column="3"
                    Padding="16,6"
                    Style="{StaticResource PrimaryButtonStyle}"
                    IsEnabled="{Binding CanBackup}"
                    Content="{Binding BackupButtonText}"
                    Click="BackupButton_OnClick" />
        </Grid>

        <!-- Status bar -->
        <Border Grid.Row="5"
                Margin="0,8,0,0"
                Padding="10"
                Background="#FFF4F4F4"
                BorderBrush="#FFD0D0D0"
                BorderThickness="1"
                CornerRadius="6">
            <TextBlock Text="{Binding StatusMessage}"
                       TextWrapping="Wrap" />
        </Border>

        <!-- Busy overlay -->
        <Grid Grid.Row="0"
              Grid.RowSpan="6"
              Background="#CC202020"
              Visibility="{Binding IsBusy, Converter={StaticResource BoolToVisibility}}">
            <StackPanel HorizontalAlignment="Center"
                        VerticalAlignment="Center">
                <ProgressBar IsIndeterminate="True"
                             Width="300"
                             Height="6" />
                <TextBlock Margin="0,16,0,0"
                           Foreground="White"
                           FontSize="15"
                           HorizontalAlignment="Center"
                           Text="{Binding BusyMessage}" />
            </StackPanel>
        </Grid>
    </Grid>
</Window>
